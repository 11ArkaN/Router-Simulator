# Repository generators hash and compare source bytes so a checkout must expose
# the same text on Windows, Linux and macOS. Git still detects binary files and
# leaves their bytes untouched, while every detected text file uses canonical
# LF in both the index and working tree. This also keeps shell scripts directly
# executable by Linux runners without maintaining a fragile extension list.
* text=auto eol=lf
