version: 4
byte_order: little-endian
fields:
  - magic
  - checkpoint_schema_hash
  - profile_hash
  - build_hash
  - hardware_deadlines
  - running_configuration
  - candidate_configuration
  - running_unsaved
  - hardware_inventory
  - link_signals
  - project_hosts
  - project_links
  - port_counters
  - operational_counters
  - arp_entries_with_expiry
  - connected_route_activation_times
  - static_route_activation_times
  - port_operational_transition_state
  - active_link_alarm_state
  - cli_session_workflow_and_context
  - fib_generation
  # Forwarding data is structural. Frames contain encoded bytes and relative
  # deadlines. Packet handles, pthread state and steady-clock epochs are absent.
  - forwarding_adjacencies
  - forwarding_arp_request_state
  - forwarding_endpoint_state
  - forwarding_transmitter_deadlines
  - forwarding_frames_and_queue_stages
